"""Replay a labeled recording through the pipeline and print the gate metrics.

Usage (once implemented):
    python scripts/evaluate.py <recording> <labels.csv> <profile.pkl>
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("evaluate script — replay + print recall / false-alarms / latency.")


if __name__ == "__main__":
    main()
