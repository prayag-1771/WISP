# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# wisp data / artifacts (keep these out of git — logs are large & private)
*.csi
*.log
data/
recordings/
profiles/
*.pkl

# generated plot output (reproducible via scripts/plot_run.py)
run.png
*.png

# local scratch (build scripts here can contain Wi-Fi credentials)
.scratch/

# Windows shortcuts are machine-local, not project files
*.lnk
